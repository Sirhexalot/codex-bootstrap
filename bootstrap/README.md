# Bootstrap Internals

This folder is implementation-only.

- `commands/` holds internal command implementations such as project initialization.
- `lib/` holds shell libraries for Tools, Skills, and MCPs.
- `templates/` holds the customer document templates.
- `catalogs/` is reserved for extracted catalog data.

User-facing onboarding belongs in the repo `README.md`, not here.
